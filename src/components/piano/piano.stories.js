import { storiesOf } from '@storybook/polymer';
import { NodeInput, GraphNode, math, input } from '@virtuaw/graphnodes';

const stories = storiesOf('Piano', module);

stories.add('default', () => `<vaw-piano></vaw-piano`);

